<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <header class="nav-header-container"><%- include('partials/header')%></header>
  <body>
    <section class="upper">
      <h1 class="header-text">Delete: <%= manufacture.name %></h1>
    </section>
    <section class="content">
        <% if (manufactures_vehicles.length) { %>
         <h2><strong>You must delete all the vehicles manufactured by <%= manufacture.name %> before attempting to delete this manufacture.</strong></h2>
         <h2><%= manufacture.name %>'s  Vehicles:</h2>
         <div class="card-container">
            <% manufactures_vehicles.forEach(vehicle => { %>
            <div class="card">
              <a href="<%-vehicle.url%>"><%= vehicle.model %></a>
              <p id="card-price">$<%= vehicle.price %></p>
            </div>
            <% }) %>
          </div>
        <% } else { %>
            <p>Are you sure you want to delete this manufacture?</p>
            <form action="" method="post">
                <input type="hidden" name="manufactureid" required="true" value="<%= manufacture._id %>">
                <button id="delete-button" type="submit">DELETE</button>
            </form>
        <% } %> 
    </section>
  </body>
  <script src="/javascripts/script.js"></script>
</html>
